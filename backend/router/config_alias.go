package router

import (
	"itdb-backend/config"
	"itdb-backend/internal/domain"
)

type Config = config.Config

// SessionUser is the domain authentication session model.
type SessionUser = domain.SessionUser
